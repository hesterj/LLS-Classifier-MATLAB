%This is the group project for COT 4501
%So far, it takes input files of numbers (floats) and puts them into a
%matlab matrix that matches the number of rows and columns of the original
%file.  So, we need to implement the algorithm for  the data generated in
%this file and convert the iris, wine, and survey datasets to the
%appropriate format.  (The format is numbers separated by spaces)

type input.txt; %reads input.txt from the appropriate folder and prints to matlab


input = fopen('input.txt','r'); %open file for reading


 %%%
 %This computes the number of columns

 marker = ' '; 
 fid = fopen('input.txt','rt');
 tLines = fgets(fid);
 columns = numel(strfind(tLines,marker)) + 1;
 fclose(fid); 
 
 %%%
 %  This computes the number of rows
 % Could be useful when choosing training set
 
  c = 0;
 fgetl(input);
 while ~feof(input)    
    fgetl(input);
    c = c+1;
 end
 
 numrows = c+1;
 
 %%%
 
input = fopen('input.txt','r');  %open the file again (just to be safe)
 


formatSpec = '%f';  %specify format (floats)

A = fscanf(input, formatSpec, [columns,Inf]);  %Scans all the columns and puts the info into matrix A
A=A' %transpose so it matches the original dimensions

fclose(input);

